// This file can be replaced during build by using the `fileReplacements` array.
// `ng build ---prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: true,
  firebase: {
    apiKey: 'AIzaSyCM-tmmRVD70zPHSg783jmACm9LPZp-Gpo',
    authDomain: 'moduloadministrativo.firebaseapp.com',
    databaseURL: 'https://moduloadministrativo.firebaseio.com',
    projectId: 'moduloadministrativo',
    storageBucket: 'moduloadministrativo.appspot.com',
    messagingSenderId: '725519080221'
  }
};

/*
 * In development mode, for easier debugging, you can ignore zone related error
 * stack frames such as `zone.run`/`zoneDelegate.invokeTask` by importing the
 * below file. Don't forget to comment it out in production mode
 * because it will have a performance impact when errors are thrown
 */
 // Included with Angular CLI.
